"""icarus.data — data adapter modules ported from v4.2.

Currently houses verbatim copy-back of v4.2's price_feed.py per the
archive-first protocol. Adapter envelope adaptation is deferred to the
parallel `data_adapters/` stream.
"""
